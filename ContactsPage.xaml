<UserControl x:Class="XqueezeOS.Views.ContactsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Height="Auto" Width="Auto">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- Left panel: Add Contact -->
        <StackPanel Grid.Column="0">
            <TextBlock Text="Add Contact" FontSize="22" FontWeight="Bold" Margin="0 0 0 20"/>

            <TextBox x:Name="NameBox" PlaceholderText="Full Name" Margin="0 10"/>
            <TextBox x:Name="PhoneBox" PlaceholderText="Phone Number" Margin="0 10"/>
            <TextBox x:Name="EmailBox" PlaceholderText="Email Address" Margin="0 10"/>

            <Button Content="Save Contact" Click="SaveContact_Click" Height="40" Margin="0 20"/>
        </StackPanel>

        <!-- Right panel: Contact List -->
        <StackPanel Grid.Column="1">
            <TextBlock Text="Saved Contacts" FontSize="22" FontWeight="Bold" Margin="0 0 0 20"/>
            <ListBox x:Name="ContactsList" Height="300"/>
            <TextBlock x:Name="SizeText" FontSize="16" Margin="0 20"/>
        </StackPanel>

    </Grid>
</UserControl>
