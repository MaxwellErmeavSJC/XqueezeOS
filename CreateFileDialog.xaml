<Window x:Class="XqueezeOS.CreateFileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create New File" 
        Height="400" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        Background="#2C2C2C"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="100"/>
        </Style>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#3C3C3C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#3C3C3C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="Padding" Value="8"/>
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- File Name -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="File Name:" 
                       Foreground="White" 
                       FontSize="14" 
                       Margin="0,0,0,5"/>
            <TextBox x:Name="txtFileName" 
                     Style="{StaticResource ModernTextBox}"
                     Text="NewFile"/>
        </StackPanel>

        <!-- File Type -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="File Type:" 
                       Foreground="White" 
                       FontSize="14" 
                       Margin="0,0,0,5"/>
            <ComboBox x:Name="cmbFileType" 
                      Style="{StaticResource ModernComboBox}"
                      SelectedIndex="0">
                <ComboBoxItem Content="Text File (.txt)"/>
                <ComboBoxItem Content="JSON File (.json)"/>
                <ComboBoxItem Content="XML File (.xml)"/>
                <ComboBoxItem Content="HTML File (.html)"/>
                <ComboBoxItem Content="CSV File (.csv)"/>
            </ComboBox>
        </StackPanel>

        <!-- File Content -->
        <StackPanel Grid.Row="2" Margin="0,0,0,5">
            <TextBlock Text="File Content:" 
                       Foreground="White" 
                       FontSize="14" 
                       Margin="0,0,0,5"/>
        </StackPanel>

        <TextBox x:Name="txtFileContent" 
                 Grid.Row="3"
                 Style="{StaticResource ModernTextBox}"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Text="Enter file content here..."
                 Margin="0,0,0,15"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button x:Name="btnCreate" 
                    Content="Create" 
                    Style="{StaticResource ModernButton}"
                    Click="BtnCreate_Click"/>
            <Button x:Name="btnCancel" 
                    Content="Cancel" 
                    Style="{StaticResource ModernButton}"
                    Background="#555555"
                    Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
